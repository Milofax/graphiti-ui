# ============================================
# Graphiti UI - Environment Variables
# ============================================
# Kopiere diese Datei nach .env:
#   cp .env.example .env
#
# Für lokale Entwicklung mit docker-compose.example.yml

# -------------------------------------------
# API Endpoints
# -------------------------------------------
# Graphiti MCP Server URL (intern im Docker-Netzwerk)
GRAPHITI_MCP_URL=http://graphiti-mcp:8000

# Graphiti MCP Server URL (extern, für Dashboard-Anzeige)
GRAPHITI_MCP_EXTERNAL_URL=http://localhost:8000

# Container Name für MCP Server Restart
GRAPHITI_MCP_CONTAINER=graphiti-mcp

# FalkorDB Browser URL (für externe Links im UI)
FALKORDB_BROWSER_URL=http://localhost:3000

# -------------------------------------------
# FalkorDB Connection (für Visualisierung/Query)
# -------------------------------------------
FALKORDB_HOST=falkordb
FALKORDB_PORT=6379
FALKORDB_PASSWORD=
FALKORDB_DATABASE=graphiti

# -------------------------------------------
# LLM Configuration (für Status-Anzeige)
# -------------------------------------------
# API URL (OpenAI-kompatibel: Ollama, OpenAI, Claude Proxy)
OLLAMA_API_URL=http://localhost:11434/v1

# API Key (für Ollama: beliebiger Wert, für OpenAI: echter Key)
OLLAMA_API_KEY=sk-ollama

# LLM Model Name
LLM_MODEL=claude

# -------------------------------------------
# Embedding Configuration (für Status-Anzeige)
# -------------------------------------------
# Embedding Model
EMBEDDING_MODEL=nomic-embed-text

# Embedding Dimensionen (MUSS zum Model passen!)
# - nomic-embed-text: 768
# - all-minilm: 384
# - text-embedding-3-small: 1536
EMBEDDING_DIM=768

# -------------------------------------------
# Authentication
# -------------------------------------------
# Admin Benutzername
ADMIN_USERNAME=admin

# JWT Secret Key (wenn leer: wird automatisch generiert)
# Manuell generieren: openssl rand -hex 32
SECRET_KEY=

# JWT Token Gültigkeit in Minuten (default: 480 = 8 Stunden)
JWT_EXPIRE_MINUTES=480

# -------------------------------------------
# Config File Path
# -------------------------------------------
# Pfad zur config.yaml (im Container)
CONFIG_PATH=/config/config.yaml

# -------------------------------------------
# Development Options
# -------------------------------------------
# Debug-Modus (true/false)
DEBUG=false

# ============================================
# MCP Server Variablen (für docker-compose)
# ============================================
# Diese werden an den MCP Server Container übergeben:
#
# OPENAI_API_KEY=sk-ollama
# OPENAI_BASE_URL=http://host.docker.internal:11434/v1
